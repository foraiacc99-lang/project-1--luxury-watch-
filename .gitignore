# Dependencies
node_modules/

# Secrets / environment (none needed today, but never commit these if added later)
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Local screenshots / scratch
temporary screenshots/
